version: '3.9'

#This is a test config
logger:
  level: "info"

storage: "sql"
#storage: "memory"

grpc:
  host: "localhost"
  port: "8000"

http:
  host: "localhost"
  port: "8080"

dbInfo:
  user: "postgres"
  password: "test"
  name: "test"
  host: "localhost"
  port: "5432"